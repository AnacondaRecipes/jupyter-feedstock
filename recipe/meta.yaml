{% set name = "jupyter" %}
{% set version = "1.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: jupyter-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: d55467bceabdea49d7e3624af7e33d59c37fff53ed3a350e1ac957bed731de7a

build:
  number: 0

requirements:
  host:
    - python
    - pip
    - setuptools
    - wheel
  run:
    - python
    - ipykernel
    - ipywidgets       # [not s390x]
    - jupyter_console
    - jupyterlab       # [not s390x]
    - nbconvert
    - notebook         # [not s390x]
    - qtconsole        # [not s390x]

test:
  requires:
    - pip
  commands:
    - pip check || true    # [s390x]
    - pip check            # [not s390x]
    - jupyter --help
    - jupyter console -h
    - jupyter lab -h       # [not s390x]
    - jupyter notebook -h  # [not s390x]

about:
  home: https://jupyter.org
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Jupyter metapackage. Install all the Jupyter components in one go.
  description: |
    Jupyter Notebook is a web application, a browser-based tool for interactive authoring of documents
    which combine explanatory text, mathematics, computations and their rich media output.
  doc_url: https://docs.jupyter.org
  dev_url: https://github.com/jupyter/jupyter

extra:
  recipe-maintainers:
    - minrk
    - ocefpaf
